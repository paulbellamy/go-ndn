package ndn

import "testing"

func Test_TCPFace(t *testing.T) {
	t.Error("pending")
	/*
		server := testServer(t)
		defer server.Close()

		subject := NewTCPFace(server.Addr().String())
		assert.IsType(t, (*tcpFace)(nil), subject)
	*/
}
